#import '../MessagesFeed/fragment.graphql'


query sendMessageInfo($id: String!) {
    match(id: $id) {
        _id
        lastMessage {
            ...messageListFields
        }
        messages {
           ...messageListFields
        }
        lastActivityDate
    }
    profile {
        user {
            _id
        }
    }
}