#import './fragment.graphql'


query matchesQuery {
    matches {
        _id
        lastMessage {
            ...lastMessageMatchFields
        }
        lastActivityDate
        person {
            _id
            name
            smallPhoto
        }
    }
}