#import '../MessagesFeed/fragment.graphql'


mutation sendMessageMutation($id: String!, $rawMessage: MessageInput!) {
    sendMessage(id: $id, rawMessage: $rawMessage) {
        ...messageListFields
    }
}