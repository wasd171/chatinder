machine:
  node: node
dependencies:
  cache_directories:
    - "~/.npm"
    - "~/Library/Caches/electron/"
compile:
  override:
    - npm run make
general:
  branches:
    only:
      - feature/configure-circleci