machine:
  node: node
  pre:
    - mkdir ~/Library/Caches/electron
dependencies:
  cache_directories:
    - ~/.npm
    - ~/Library/Caches/electron
compile:
  override:
    - npm run publish
general:
  branches:
    only:
      - master