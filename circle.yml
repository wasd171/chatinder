machine:
  node: node
  pre:
    - mkdir ~/Library/Caches/electron
dependencies:
  pre:
    npm run circle:clean
  cache_directories:
    - ~/.npm
    - ~/Library/Caches/electron
compile:
  override:
    - npm run publish
general:
  branches:
    only:
      - feature/check-ci