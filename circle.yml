machine:
  node: node
  pre:
    - mkdir ~/Library/Caches/electron
dependencies:
  cache_directories:
    - ~/Library/Caches/electron
compile:
  override:
    - npm run make
general:
  branches:
    only:
      - feature/configure-circleci